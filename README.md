# React Starter (Security + UI)

- React + Vite + TypeScript
- Tailwind con **tokens CSS variables** y light/dark
- ESLint/Prettier
- Playwright smoke test
- Pensado para integrarse con **Agent OS** (módulos `core/ui-rules.md`, `ui/tokens.md`, etc.).

## Run
npm install
npm run dev

## Build
npm run build
npm run preview
